window.CEME_CHECKOUT = {
  // URL da API (pasta server/). Vazio = modo demonstração no navegador.
  // Ex.: "https://ceme-checkout.onrender.com"
  apiUrl: "",
  // Chave pública do Mercado Pago. Pode ficar vazia se a API devolver em /api/config.
  mpPublicKey: "",
  maxInstallments: 3,
  shippingFee: 0,
};
